_format_version: "3.0"
_transform: true

services:
  - name: fastapi-service
    url: http://api:8000
    routes:
      - name: fastapi-route
        paths:
          - /
        strip_path: false

plugins:
  - name: rate-limiting
    config:
      minute: 100
      policy: local
