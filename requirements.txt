# ==============================================================================
# requirements.txt — Sistema de Matrícula SSD
# Gerado via: poetry export -f requirements.txt --without-hashes
# Python requerido: >= 3.12
# ==============================================================================

# --- Framework Web ---
fastapi==0.111.1
starlette==0.37.2
uvicorn[standard]==0.30.6
httptools==0.7.1
websockets==16.0
watchfiles==1.1.1

# --- Validação e Configuração ---
pydantic==2.13.3
pydantic-core==2.46.3
pydantic-settings==2.14.0
python-dotenv==1.2.2
annotated-types==0.7.0
typing-extensions==4.15.0
typing-inspection==0.4.2

# --- Banco de Dados (ORM + Driver + Migrations) ---
sqlalchemy==2.0.49
asyncpg==0.29.0
alembic==1.18.4
greenlet==3.5.0
mako==1.3.11

# --- Segurança (JWT + Hashing) ---
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
cryptography==47.0.0
bcrypt==5.0.0
cffi==2.0.0
pycparser==3.0
pyasn1==0.6.3
rsa==4.9.1
ecdsa==0.19.2
six==1.17.0

# --- HTTP e Formulários ---
python-multipart==0.0.9
httpx==0.27.2
httpcore==1.0.9
h11==0.16.0
anyio==4.13.0
sniffio==1.3.1
idna==3.13
certifi==2026.4.22

# --- Utilitários CLI e Output ---
click==8.3.3
typer==0.25.0
rich==15.0.0
rich-toolkit==0.19.7
pygments==2.20.0
markdown-it-py==4.0.0
mdurl==0.1.2
shellingham==1.5.4
jinja2==3.1.6
markupsafe==3.0.3
pyyaml==6.0.3
colorama==0.4.6

# --- Email (FastAPI extras) ---
email-validator==2.2.0
dnspython==2.8.0

# --- FastAPI CLI ---
fastapi-cli==0.0.24
annotated-doc==0.0.4

# ==============================================================================
# NOTA: Dependências gerenciadas via Docker (não instaladas via pip)
# ==============================================================================
#
# --- API Gateway ---
# Kong Gateway (DB-less mode) — configurado via docker-compose.yml + docker/kong/kong.yml
# Imagem: kong:3.6-alpine
# Não requer pacotes Python; comunicação via proxy reverso HTTP.
#
# --- Banco de Dados ---
# PostgreSQL 16 — executado via Docker (imagem: postgres:16-alpine)
#
# ==============================================================================
# NOTA: Decisões de autenticação
# ==============================================================================
#
# O módulo de autenticação utilizará:
# - python-jose[cryptography]: geração e validação de tokens JWT (já incluído acima)
# - passlib[bcrypt]: hash seguro de senhas com algoritmo bcrypt (já incluído acima)
# - Modelo ORM 'Usuario' no banco com campos: id, username, email, hashed_password, role
# - Papéis disponíveis: ADMIN, ALUNO, COORDENACAO, PROCESSAMENTO
#
